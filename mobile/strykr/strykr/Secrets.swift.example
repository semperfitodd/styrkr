import Foundation

// IMPORTANT: Copy this file to Secrets.swift and add your actual credentials
// Secrets.swift should be added to .gitignore to keep credentials secure

struct Secrets {
    static let apiBaseURL = "https://YOUR_API_URL"
    static let cognitoClientId = "YOUR_COGNITO_CLIENT_ID"
    static let cognitoDomain = "YOUR_COGNITO_DOMAIN"
    static let cognitoUserPoolId = "YOUR_USER_POOL_ID"
    static let logoutUri = "YOUR_APP_SCHEME://logout"
    static let redirectUri = "YOUR_APP_SCHEME://auth/callback"
    static let exerciseLibraryURL = "https://YOUR_DOMAIN/config/exercises.latest.json"
}
